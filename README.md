To reproduce the figures from the paper (including converting these to PDFs):

1. navigate to the folder containing this file;

2. type "bash run.sh" (without the quotation marks) into the command line

The tikz files of the generated figures can then be found in /tex/fig.

The PDF files are in /output. See run.sh for more details, e.g., on how to replicate the underlying results first.
